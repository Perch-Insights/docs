{
  "name": "perch-docs",
  "private": true,
  "type": "module",
  "description": "Perch product documentation (Mintlify). Screenshots are generated by capture/, never hand-made.",
  "scripts": {
    "capture": "node capture/capture.mjs",
    "check": "mint validate && mint broken-links"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
